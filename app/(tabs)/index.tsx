import HomeScreenComponent from "@/screens/home";
import { ScrollView } from "react-native";

export default () => {
  return (
    <ScrollView>
      <HomeScreenComponent />
    </ScrollView>
  );
};
